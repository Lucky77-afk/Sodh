# Database configuration
# Uncomment and set the following if you have a PostgreSQL database
# DATABASE_URL = "postgresql://username:password@host:port/database"

# Solana RPC configuration
# You can use a public RPC endpoint or your own node
SOLANA_RPC_URL = "https://api.mainnet-beta.solana.com"
# For better performance, consider using a dedicated RPC provider:
# SOLANA_RPC_URL = "https://your-rpc-provider.com"

# Optional: If you need to sign transactions, add a wallet private key
# WALLET_PRIVATE_KEY = "your_private_key_here"

# Other API keys and secrets
# API_KEY = "your_api_key_here"
